# Phases 1-3 — topic research, script, storyboard + Sheet intake.
httpx>=0.27.0            # Anthropic Messages API (web search) over raw HTTP
gspread>=6.0.0          # Google Sheets topic queue
google-auth-oauthlib>=1.2.0
python-dotenv>=1.0.0

# Phase 4 — voice narration.
edge-tts>=6.1.0                     # free, no-key fallback voice (always available)
mutagen>=1.47.0                     # MP3 duration (pure-Python, no ffmpeg needed)
google-cloud-texttospeech>=2.16.0   # Google Cloud TTS fallback provider
# ElevenLabs (primary) uses the REST API over httpx — no extra package needed.

# Phase 8 — thumbnails + metadata.
pillow>=10.0.0          # thumbnail text overlay / compositing (1280x720)
# A heavy/bold TTF must be installed (default: Liberation Sans Bold). Override
# with DOC_THUMB_FONT. Jamendo/Claude use httpx (already listed).
