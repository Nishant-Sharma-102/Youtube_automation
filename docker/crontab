# supercronic crontab (runs inside the container, timezone = TZ env = Asia/Kolkata).
# Daily documentary produce + publish at 08:00 IST. Output goes to supercronic's stdout
# (visible via `docker logs`). The script itself also appends to /app/logs.
0 8 * * * /app/scripts/cron-documentary.sh
